# KitCerto - Environment Variables Example
# Copy this file to .env and adjust the values

# ===========================================
# API Configuration
# ===========================================
ASPNETCORE_URLS=http://localhost:5000
ASPNETCORE_ENVIRONMENT=Development

# ===========================================
# Database (MongoDB)
# ===========================================
ConnectionStrings__Mongo=mongodb://localhost:27017/kitcerto
Mongo__DatabaseName=kitcerto

# ===========================================
# Authentication (Keycloak)
# ===========================================
Auth__Authority=http://localhost:8080/realms/kitcerto
Auth__Audience=kitcerto-api
Auth__RequireHttpsMetadata=false
# (Swagger OAuth) Realm (used by SwaggerServiceExtensions)
Auth__Realm=kitcerto

# ===========================================
# CORS Configuration
# ===========================================
Cors__Origins=http://localhost:3000;http://localhost;http://frontend:3000

# ===========================================
# Frontend Configuration
# ===========================================
# Para rodar sem Nginx, a API expõe /api
NEXT_PUBLIC_API_BASE_URL=http://localhost:5000/api
NEXT_PUBLIC_USE_MOCKS=false
NEXT_PUBLIC_KEYCLOAK_URL=http://localhost:8080
NEXT_PUBLIC_KEYCLOAK_REALM=kitcerto
NEXT_PUBLIC_KEYCLOAK_CLIENT_ID=kitcerto-frontend

# ===========================================
# Docker Compose (Development)
# ===========================================
# When using docker-compose.dev.yml, these are set automatically:
# - API connects to: mongodb://mongo:27017/kitcerto
# - API connects to Keycloak: http://keycloak:8080/realms/kitcerto
# - Frontend connects to API: http://api:5000
# - Frontend connects to Keycloak: http://keycloak:8080

# ===========================================
# Docker Compose (Prod-like, infra/docker-compose.yml)
# ===========================================
# Keycloak with Postgres persistence
KC_DB=postgres
KC_DB_URL=jdbc:postgresql://postgres:5432/keycloak
KC_DB_USERNAME=keycloak
KC_DB_PASSWORD=keycloak
